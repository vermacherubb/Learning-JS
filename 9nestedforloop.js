// nested for loop
// print lower triangular matrix using nested for loop 

for (let r = 1; r <= 5; r++) 
    {
        for(let c=1;c<=r;c++)
        {
            document.write("*")
        }
        document.write("<br>")
    
}
